import React, { Fragment } from 'react';
import Header from '../Header/Header';

const dashboard = () => (
    <Fragment>
        <Header />
        <h1>dashboard</h1>
    </Fragment>
)

export default dashboard;