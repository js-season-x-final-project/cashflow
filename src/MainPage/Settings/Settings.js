import React, { Fragment } from 'react';
import Header from '../Header/Header';

const settings = () => (
    <Fragment>
        <Header />
        <h1>Settings</h1>
    </Fragment>
)

export default settings;