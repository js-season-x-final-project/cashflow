import React, { Fragment } from 'react';
import { withRouter } from 'react-router';

const blog = props => {

    return(
        <Fragment>
            {/* <Header/> */}
            <h1>Blog</h1>
        </Fragment>
    )
}

export default withRouter(blog);