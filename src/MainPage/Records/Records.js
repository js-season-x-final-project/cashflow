import React, { Fragment } from 'react';
import Header from '../Header/Header';

const records = () => (
    <Fragment>
        <Header />
        <h1>Records</h1>
    </Fragment>
)

export default records;